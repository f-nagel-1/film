from django.urls import path
from . import views

urlpatterns = [
    #path('/homepage', views.rental, name = 'homepage'),
    #path('rental/<int:rental_id>', views.show_rental, name = 'show_rental'),
    #path('add_customers', views.add_customers, name = 'add_customers'),

]